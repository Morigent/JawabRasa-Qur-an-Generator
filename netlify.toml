[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

# SPA fallback — all paths serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
